# Process Cross-Validation Metrics from Results List converts the nested list of cross-validation metrics into a long-format data frame

Process Cross-Validation Metrics from Results List converts the nested
list of cross-validation metrics into a long-format data frame

## Usage

``` r
process_cross_validation_metrics(results_models, classifiers)
```
