# Internal helper to handle directory creation and saving .Rda files

Internal helper to handle directory creation and saving .Rda files

## Usage

``` r
save_helper_rda(data_obj, obj_name, filename, directory, subfolder = "")
```
