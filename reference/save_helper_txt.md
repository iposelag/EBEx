# Internal helper to handle directory creation and saving text files

Internal helper to handle directory creation and saving text files

## Usage

``` r
save_helper_txt(data, filename, directory, subfolder = "")
```
