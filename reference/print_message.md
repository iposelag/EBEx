# Print Timestamped Messages

Print Timestamped Messages

## Usage

``` r
print_message(..., verbose = getOption("EBEx.verbose", default = TRUE))
```

## Arguments

- ...:

  Arguments to be passed to the message.

- verbose:

  Logical. If FALSE, the message is suppressed. Defaults to the global
  option "EBEx.verbose" or TRUE.
