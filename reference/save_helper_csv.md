# Internal helper to handle directory creation and saving CSV files

Internal helper to handle directory creation and saving CSV files

## Usage

``` r
save_helper_csv(data, filename, directory, subfolder = "", verbose = TRUE)
```
