# Internal helper to save multiple objects into one .Rda file

Internal helper to save multiple objects into one .Rda file

## Usage

``` r
save_helper_multi_rda(
  obj_list,
  name_list,
  filename,
  directory,
  subfolder = "",
  verbose = TRUE
)
```
